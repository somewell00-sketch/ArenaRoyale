GitHub Pages setup (to avoid 'Unexpected token export'):
Option 1 (recommended): Settings → Pages → Deploy from branch → main → /(root)
Option 2: Settings → Pages → main → /docs

This package includes BOTH root/ and docs/ with the same site files.
Open the Pages URL, not individual JS files.
